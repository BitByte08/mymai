# Cloudflare Tunnel Token
# Cloudflare Zero Trust → Networks → Tunnels → Create Tunnel → Docker
CF_TUNNEL_TOKEN=your-tunnel-token-here

# PostgreSQL (required by docker-compose; use a real secret locally)
POSTGRES_DB=carol
POSTGRES_USER=carol
POSTGRES_PASSWORD=replace-with-a-long-random-secret
DATABASE_URL=postgresql://carol:replace-with-a-long-random-secret@postgres:5432/carol

# 컨테이너 메모리 상한 (cgroup 레벨). 앱 버그로 메모리가 폭증해도 이 컨테이너만 OOM-kill되어
# restart 정책으로 재기동하도록 막아주는 안전장치 — 없으면 호스트 전체 메모리를 다 먹어서
# SSH 접속까지 막힐 수 있음. 기본값은 `free -h` 기준 총 952MB짜리 VM에 맞춘 것
# (256+400+64=720MB, 호스트 OS용으로 ~230MB 남김). 세 값의 합이 VM 총 메모리보다
# 항상 작아야 함 — 안 그러면 개별 컨테이너 제한이 있어도 다 합쳐서 호스트를 굶길 수 있음.
POSTGRES_MEM_LIMIT=256m
BOT_MEM_LIMIT=400m
CLOUDFLARED_MEM_LIMIT=64m

# bot 컨테이너 메인 스레드 V8 힙 상한(MB). NODE_MAX_OLD_SPACE_MB + 아래 RENDER_POOL_SIZE *
# 워커별 힙(128MB) + Node 자체 오버헤드가 BOT_MEM_LIMIT를 넘지 않게 유지할 것.
NODE_MAX_OLD_SPACE_MB=160

# 렌더(satori/resvg) 워커 스레드 개수. 워커 하나당 별도 V8 힙(renderPool.ts의
# resourceLimits, 기본 128MB)을 쓰므로 BOT_MEM_LIMIT 안에서 메인 스레드 힙(위
# NODE_MAX_OLD_SPACE_MB)과 합쳐 여유가 있는지 계산해서 올릴 것. 지금 VM 크기에서는 1 권장.
RENDER_POOL_SIZE=1

# XRSS (패치노트 소스) — X 계정 자격증명
# 공식 계정이 아닌 스크레이핑 전용 버너 계정 권장. 트윗을 "읽기"만 하는 용도.
TWITTER_USERNAME=your-x-handle
TWITTER_EMAIL=your-x-email
TWITTER_PASSWORD=your-x-password
# 2FA(TOTP)가 켜진 계정만 필요. 없으면 비워두면 됨.
TWITTER_TOTP_SECRET=
